# Changesets

This folder holds pending release notes for the monorepo.

Run `npm run changeset` from the repo root to add a changeset when a PR should bump a package version. See the **Release** section in the root [README](../README.md).
